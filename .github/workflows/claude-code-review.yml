name: Claude Code Review

on:
  pull_request_target:
    types: [ready_for_review]

permissions:
  statuses: write

jobs:
  trigger-review:
    uses: minghsuy/dgx-infra/.github/workflows/claude-review-bridge.yml@514c15797d04d700dd13e3c06a7a85b45dab3926
    with:
      runs-on: '"ubuntu-latest"'
    secrets:
      BRIDGE_PAT: ${{ secrets.BRIDGE_PAT }}
